/**
 * Search-related constants used throughout the SDK
 */

/**
 * Lowercase string representation of collection source for backwards compatibility
 * Used when checking string values against SearchSource.COLLECTION
 */
export const COLLECTION_SOURCE_LOWERCASE = 'collection';

/**
 * Default sort order for search results
 */
export const DEFAULT_SORT_BY = 'relevance';

/**
 * Default search path for URL navigation
 */
export const SEARCH_PATH = '/search';

/**
 * Default product type for search queries
 */
export const DEFAULT_TYPE = 'product';
