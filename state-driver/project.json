{
  "name": "state-driver-sdk",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "cd state-driver && vite build",
            "forwardAllArgs": false
          }
        ]
      }
    }
  }
}